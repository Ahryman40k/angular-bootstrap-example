{
  "name": "mongodb",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "docker/mongo",
  "projectType": "application",
  "targets": {
    "docker": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t mongo-agir -f ./docker/mongo/Dockerfile ."
      }
    }
  },
  "tags": ["infra"]
}
