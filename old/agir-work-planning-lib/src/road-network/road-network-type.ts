/**
 * Enum containing the full list of all possible road network types.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum RoadNetworkType {
  arterial = 'arterial',
  arterialLocal = 'arterialLocal',
  local = 'local',
  offRoadNetwork = 'offRoadNetwork'
}
