/**
 * Enum containing the full list of all possible medal types.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum MedalType {
  platinum = 'platinum',
  gold = 'gold',
  silver = 'silver',
  bronze = 'bronze'
}
