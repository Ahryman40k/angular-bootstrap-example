// tslint:disable: no-unused-expression
import { expect } from 'chai';
import { IEnrichedIntervention } from './';

describe('Index', () => {
  it('should import', () => {
    const i: IEnrichedIntervention = {} as IEnrichedIntervention;
    expect(i).to.not.be.undefined;
  });
});
