/**
 * Enum containing the full list of all possible program book expand parameter.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum ProgramBookExpand {
  annualProgram = 'annualProgram',
  projects = 'projects',
  projectsInterventions = 'projectsInterventions',
  removedProjects = 'removedProjects'
}
