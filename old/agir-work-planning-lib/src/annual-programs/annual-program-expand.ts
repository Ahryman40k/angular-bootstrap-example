/**
 * Enum containing the full list of all possible project expand parameter.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum AnnualProgramExpand {
  programBooks = 'programBooks'
}
