/**
 * Enum containing the full list of all possible annual program constants.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum AnnualProgramConstant {
  maximumYear = new Date().getFullYear() + 10,
  minimumYear = 2010
}
