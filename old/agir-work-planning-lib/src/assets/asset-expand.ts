/**
 * Enum containing the full list of all possible asset expand parameter.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum AssetExpand {
  workArea = 'workArea',
  roadSections = 'roadSections',
  assetDetails = 'assetDetails',
  suggestedStreetName = 'suggestedStreetName'
}
