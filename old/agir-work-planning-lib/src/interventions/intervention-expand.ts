/**
 * Enum containing the full list of all possible intervention expand parameter.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum InterventionExpand {
  annualProgram = 'annualProgram',
  programBook = 'programBook',
  project = 'project',
  assets = 'assets'
}
