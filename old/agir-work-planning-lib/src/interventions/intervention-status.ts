/**
 * Enum containing the full list of all possible intervention statuses.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum InterventionStatus {
  accepted = 'accepted',
  canceled = 'canceled',
  deleted = 'deleted',
  integrated = 'integrated',
  refused = 'refused',
  waiting = 'waiting',
  wished = 'wished'
}
