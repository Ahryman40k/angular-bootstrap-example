/**
 * Enum containing the full list of all possible integration notices followUp types.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum OpportunityNoticeResponseRequestorDecision {
  analyzing = 'analyzing',
  no = 'no',
  yes = 'yes'
}
