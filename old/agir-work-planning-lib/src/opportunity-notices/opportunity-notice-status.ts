/**
 * Enum containing the full list of all possible integration notices statuses.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum OpportunityNoticeStatus {
  new = 'new',
  inProgress = 'inProgress',
  closed = 'closed'
}
