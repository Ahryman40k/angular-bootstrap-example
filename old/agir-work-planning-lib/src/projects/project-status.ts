/**
 * Enum containing the full list of all possible project statuses.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum ProjectStatus {
  canceled = 'canceled',
  finalOrdered = 'finalOrdered',
  planned = 'planned',
  postponed = 'postponed',
  preliminaryOrdered = 'preliminaryOrdered',
  programmed = 'programmed',
  replanned = 'replanned'
}
