/**
 * Enum containing the full list of all possible project sub-category.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum ProjectSubCategory {
  priority = 'priority',
  recurrent = 'recurrent',
  successive = 'successive',
  urgent = 'urgent'
}
