/**
 * Enum containing the full list of all possible project decision types.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum ProjectType {
  integrated = 'integrated',
  integratedgp = 'integratedgp',
  nonIntegrated = 'nonIntegrated',
  other = 'other'
}
