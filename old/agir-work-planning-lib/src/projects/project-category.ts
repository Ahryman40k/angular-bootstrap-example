/**
 * Enum containing the full list of all possible project category.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum ProjectCategory {
  completing = 'completing',
  new = 'new',
  postponed = 'postponed'
}
