/**
 * Enum containing the full list of all possible document statuses.
 *
 * *Keep it sorted alphabetically. (If using VSCode, use "Sort Lines Ascending" command)*
 */
export enum DocumentStatus {
  pending = 'pending',
  refused = 'refused',
  validated = 'validated'
}
